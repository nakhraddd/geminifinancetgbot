# ⚡ Quick Start Guide

## Быстрая установка (5 минут)

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
# 1. Запустить скрипт установки
./setup.sh

# 2. Активировать виртуальное окружение
source venv/bin/activate

# 3. Проверить установку
python check_setup.py
```

### Вариант 2: Ручная установка

```bash
# 1. Создать виртуальное окружение
python3 -m venv venv

# 2. Активировать
source venv/bin/activate  # Mac/Linux
# или venv\Scripts\activate  # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Проверить установку
python check_setup.py
```

## Первые шаги

### 1. Настройка API ключа

Убедитесь что в `.env` файле указан ваш Google API ключ:

```bash
GOOGLE_API_KEY=your_actual_api_key_here
```

### 2. Генерация тестовых данных

```bash
python generate_data.py
```

Это создаст 40 тестовых сценариев в `src/data/synthetic_conversations.json`

### 3. Тестирование агента

#### Интерактивный режим:

```bash
python test_agent.py
```

Примеры вопросов:
- "Рассчитай зарплату на руки при окладе 500000 тенге"
- "Могу ли я принять НДС к вычету по аренде офиса?"
- "У меня оклад 300к, двое детей, сколько налогов?"

#### Программный режим:

```python
from src.agent.agent import AccountingAgent

agent = AccountingAgent()
result = agent.answer("Рассчитай зарплату 500000 тенге")
print(result['response'])
```

### 4. Запуск Evaluation

```bash
python run_evals.py
```

Это запустит:
- ✅ Correctness тесты (расчет зарплаты + НДС консультации)
- 🛡️ Safety тесты (adversarial + edge cases)
- 📊 Генерацию отчета в `results/report.md`

## Структура результатов

После запуска `run_evals.py` в папке `results/` появятся:

```
results/
├── salary_correctness_results.json      # Детальные результаты зарплатных расчетов
├── vat_correctness_results.json         # Результаты НДС консультаций
├── adversarial_safety_results.json      # Проверка на устойчивость к опасным запросам
├── edge_case_safety_results.json        # Edge cases
└── report.md                             # Итоговый markdown отчет
```

## Примеры использования

### Пример 1: Простой расчет зарплаты

```python
from src.agent.agent import AccountingAgent

agent = AccountingAgent()
result = agent.answer("Рассчитай зарплату на руки при окладе 500000 тенге")

print(result['response'])
# Выведет подробный расчет с ОПВ, ИПН, ВОСМС и итоговой суммой
```

### Пример 2: Консультация по НДС

```python
agent = AccountingAgent()
result = agent.answer("Можно ли принять НДС к вычету по корпоративному мероприятию?")

print(result['response'])
# Выведет консультацию со ссылкой на НК РК
```

### Пример 3: Генерация своих тестовых данных

```python
from src.utils.data_generator import SyntheticDataGenerator

generator = SyntheticDataGenerator()
data = generator.generate_all(salary_count=50, vat_count=50)

print(f"Создано {data['salary'].__len__()} сценариев зарплаты")
print(f"Создано {data['vat'].__len__()} сценариев НДС")
```

### Пример 4: Использование Evaluator

```python
from src.evals.evaluator import GeminiEvaluator

evaluator = GeminiEvaluator()

# Оценить ответ по зарплате
eval_result = evaluator.evaluate_salary_correctness(
    user_input="Рассчитай зарплату 500000",
    agent_response="<ответ агента>",
    expected={
        'context': {'gross_salary': 500000, 'dependents': 0},
        'expected_calculations': {
            'opv': 50000,
            'vosms': 10000,
            'taxable_income': 331000,
            'ipn': 33100,
            'net_salary': 406900
        }
    }
)

print(f"Pass: {eval_result['pass']}")
print(f"Score: {eval_result['score']}/100")
print(f"Critique: {eval_result['critique']}")
```

## Полезные команды

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Деактивировать
deactivate

# Проверить установку
python check_setup.py

# Генерация данных
python generate_data.py

# Тестирование агента
python test_agent.py

# Запуск evaluation
python run_evals.py

# Запуск только correctness тестов
pytest src/evals/test_correctness.py -v

# Запуск только safety тестов
pytest src/evals/test_safety.py -v

# Запуск всех тестов
pytest src/evals/ -v
```

## Устранение проблем

### Ошибка: "GOOGLE_API_KEY not found"

**Решение:** Проверьте файл `.env` и убедитесь что API ключ указан:
```bash
GOOGLE_API_KEY=AIzaSy...  # ваш реальный ключ
```

### Ошибка: "Module not found"

**Решение:** Убедитесь что виртуальное окружение активировано и зависимости установлены:
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Ошибка: "synthetic_conversations.json not found"

**Решение:** Сгенерируйте тестовые данные:
```bash
python generate_data.py
```

### Тесты проваливаются с низким score

**Это нормально!** Основные причины:
1. Агент может давать неполные объяснения
2. Не всегда указывает ссылки на законодательство
3. Возможны мелкие ошибки в расчетах

**Решение:** Улучшайте промпты в `src/agent/prompts.py`

## Следующие шаги

1. ✅ Проверьте что все работает с помощью `check_setup.py`
2. 🧪 Протестируйте агента с разными вопросами через `test_agent.py`
3. 📊 Запустите полный evaluation с помощью `run_evals.py`
4. 📈 Изучите отчет в `results/report.md`
5. 🔧 Улучшите промпты на основе результатов
6. 🔄 Повторите evaluation и отследите улучшения

## Дополнительные ресурсы

- **README.md** - Полная документация проекта
- **src/agent/config.py** - JTBD и конфигурация агента
- **src/agent/prompts.py** - Промпты для настройки поведения
- **results/report.md** - Детальный отчет после evaluation

---

**Нужна помощь?** Создайте issue в репозитории проекта.
